# Seed for ~/.codex/config.toml — applied by apply.sh only if no config exists.
# On machines with an existing config, merge these two keys manually.
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
